import * as React from 'react';
import { IRequestFormProps } from './IRequestFormProps';



const RequestForm: React.FunctionComponent<IRequestFormProps> = (props) => {
    return (
        <div>
            Request Form (Form Design)
        </div>
    );
};

export default RequestForm;
